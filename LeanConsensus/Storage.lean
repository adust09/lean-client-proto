/-
  Storage — RocksDB FFI + IO.Ref Cache (Phase 5 stub)

  Will implement:
  - RocksDB FFI for persistent storage
  - IO.Ref hot cache for frequently accessed state
  - Block/state serialization via SSZ
-/

namespace LeanConsensus.Storage

-- Phase 5: RocksDB FFI + caching

end LeanConsensus.Storage
